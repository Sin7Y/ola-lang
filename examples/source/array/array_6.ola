
contract ArraySortExample {
    
    fn main() {
       u32[10] source = [3, 4, 5, 1, 7, 9, 0, 2, 8, 6];
       u32[10] source2 = source;
       u32[10] sorted = array_sort_test(source2);
    }

    fn array_sort_test(u32[10] source) -> (u32[10]) {
        u32[10] array_sorted = u32_array_sort(source);
        return array_sorted;
    }
}
