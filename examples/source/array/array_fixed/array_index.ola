
contract ArrayIndexExample {
    
    fn test() {
       u32 index = 2;
       u32 value = array_index(index);
       assert(value == 3);
    }

    fn array_index(u32 index) -> (u32) {
        u32[3] array = [1, 2, 3];
        return array[index];
    }
}
