
contract StructArrayExample {
    
  
    struct Book {
        u32 book_id;
        u32 book_name;
    }


    fn createBooks() -> (Book[]) {
       
        Book[] myBooks = new Book[](1);
        myBooks[0] = Book(99, 100);
        return myBooks;
    }

}
