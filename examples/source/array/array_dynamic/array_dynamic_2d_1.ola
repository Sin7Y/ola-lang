

contract DynamicTwoDimensionalArrayInMemory {
    fn initialize(u32 rows) -> (u32[3][]) {
        u32[3][] dynamicArray = new u32[3][](rows);
        for (u32 i = 0; i < rows; i++) {
            dynamicArray[i] = [1, 2, i];
        }
        return dynamicArray;
    }

}
