contract MultInputExample {
    fn foo(u32 a, u32 b, u32 c, u32 d, u32 e, u32 f, u32 g) {  
        u32 a_b = a + b;
        u32 c_d = c + d;
        u32 e_f = e + f;
        u32 result = a_b + c_d + e_f + g;
        assert(result == 28);
    }

}