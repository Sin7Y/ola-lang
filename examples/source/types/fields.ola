contract FieldsContract {

    fn fields_test() -> (fields){
       string a = "ola";
       string b = "vm";
       fields a_b = fields_concat(a, b);
       assert(string(a_b) == "olavm");
       return a_b;
    }

}
