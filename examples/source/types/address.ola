contract AddressExample {
    address myaddress;

    fn compare_address(address _address) -> (bool) {
        return myaddress == _address;
    }

    fn u32_to_address(u32 a) -> (address) {
        u32 b = 0x0000000001;
        return address(0x1234);
    }

}
