contract Fibonacci {

    fn main() {
       fib_recursive(10);
    }

    fn fib_recursive(u32 n) -> (u32) {
        if (n < 2) {
            return 1;
        }
        return fib_recursive(n -1) + fib_recursive(n -2);
    }

}