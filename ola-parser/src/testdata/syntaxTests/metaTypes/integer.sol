contract Test {
    fn basic()  {
        u256 uintMax = type(u256).max;
        uintMax;
        int intMax = type(int).max;
        intMax;
        u256 uintMin = type(u256).min;
        uintMin;
        int intMin = type(int).min;
        intMin;
    }
}
// ----
