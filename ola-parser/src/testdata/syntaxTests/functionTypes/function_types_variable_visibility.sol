contract C {
    fn(bytes memory) a1;
    fn(bytes memory) internal b1;
    fn(bytes memory) internal internal b2;
    fn(bytes memory) external c1;
    fn(bytes memory) external internal c2;
    fn(bytes memory) external public c3;
}
// ----
