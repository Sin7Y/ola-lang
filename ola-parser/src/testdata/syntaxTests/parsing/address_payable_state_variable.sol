contract C {
    address payable a;
    address payable b;
    address payable[] c;
    address payable[] d;
    mapping(u256 => address payable) e;
    mapping(u256 => address payable[]) f;
}
// ----
